# Google Maps Platform browser key, restricted to aipfinance.com.au in
# Google Cloud Console (Websites application restriction) and scoped to
# the Places API. Used client-side to pull live Google reviews.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
